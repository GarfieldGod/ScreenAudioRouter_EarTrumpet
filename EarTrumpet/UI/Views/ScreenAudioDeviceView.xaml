<UserControl x:Class="EarTrumpet.UI.Views.ScreenAudioDeviceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Theme="clr-namespace:EarTrumpet.UI.Themes"
             xmlns:local="clr-namespace:EarTrumpet.UI.Views"
             xmlns:vm="clr-namespace:EarTrumpet.UI.ViewModels"
             xmlns:b="clr-namespace:EarTrumpet.UI.Behaviors"
             IsTabStop="False"
             x:Name="RootControl">
    <UserControl.Resources>
    <Style TargetType="{x:Type ComboBox}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="IsEditable" Value="False" />
        <Setter Property="Padding" Value="4,2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid x:Name="templateRoot" SnapsToDevicePixels="True">
                        <Border x:Name="MainBorder"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Background="{TemplateBinding Background}">
                            <ContentPresenter x:Name="ContentSite"
                                      Margin="{TemplateBinding Padding}"
                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      Content="{TemplateBinding SelectionBoxItem}"
                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                      IsHitTestVisible="False" />
                        </Border>
                        <ToggleButton x:Name="ToggleButton"
                              Grid.Column="1"
                              Background="Transparent"
                              BorderBrush="Transparent"
                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                              ClickMode="Press">
                            <Path x:Name="Arrow"
                          Width="8"
                          Height="4"
                          Margin="0,0,6,0"
                          HorizontalAlignment="Right"
                          VerticalAlignment="Center"
                          Data="M0,0 L4,4 L8,0 Z"
                          Fill="Gray" />
                        </ToggleButton>
                        <Popup x:Name="PART_Popup"
                           AllowsTransparency="True"
                           Placement="Bottom"
                           IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                           PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}">
                            <Border x:Name="SubmenuBorder"
                            Theme:Brush.Background="Theme={Theme}ChromeLow, HighContrast=Window"
                            Theme:Brush.BorderBrush="Light=LightChromeHigh, Dark=ControlDarkAppButtonTextDisabled/0.9, HighContrast=ControlText"
                            BorderThickness="1"
                            SnapsToDevicePixels="True">
                                <ScrollViewer MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                      MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    </UserControl.Resources>
    <StackPanel>
        <ItemsControl x:Name="DeviceItemsControl"
                      ItemsSource="{Binding ScreenAudioMappingList}">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:ScreenAudioDeviceItemViewModel}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding ScreenName}" Margin="20 0 0 0">
                            <TextBlock.Style>
                                <Style BasedOn="{StaticResource AppVolumeTextStyle}" TargetType="TextBlock" />
                            </TextBlock.Style>
                        </TextBlock>

                        <TextBlock Text=" : " Width="50"/>

                        <ComboBox Width="200" ItemsSource="{Binding DataContext.AudioDeviceNameList, RelativeSource={RelativeSource AncestorType=UserControl}}" SelectedItem="{Binding SelectedAudioDeviceName, Mode=TwoWay}" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </StackPanel>
</UserControl>